import { createConnection } from 'typeorm';
/* o createConnection so de chamar procura na pasta do projeto um arquivo chamado
ormconfig.json, vai ler os dados desse arquivo e vai fazer a conexao do banco
de dados. */

createConnection();
